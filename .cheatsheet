Git
    create:             git clone git@github.com:hueyjj/<project name>
    init:               git init

    status:             git status or git st
    differences:        git diff

    add all changes in this directory recursively:  git add .
    add changes interactively through patching:     git add -p
    commit changes:                                 git commit or git commit -m "message here"
    modify commit:                                  git commit --amend

    commit history:                             git log
    commit history (graph):                     git log --graph

    push changes to remote:                     git push <remote> <branch>

    list branches:                              git branch -av
    switch branch:                              git checkout <branch>
    delete local branch:                        git branch -d <branch>

    download changes from remote:               git fetch <remote>

    save commits:                               git stash
    apply saved commits:                        git stash apply

    discard all changes and go back to HEAD:    git reset --hard HEAD

    removing files from git repo:               git rm <file>

    

