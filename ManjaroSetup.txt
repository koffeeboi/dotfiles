Connect to network

select fastest mirrors to download from
$ sudo pacman-mirrors --fasttrack 

update manjaro
$ sudo pacman -Syyu

install package manager
$ sudo pacman -S yaourt

# This works for the laptop keyboard, but does not persist when reconnecting external keyboards
# If using external keyboards, just use keyboards built-in program to remap caps to control, else
# find a different solution                                                                      						
Capslock to control 				
    sudo edit /etc/default/keyboard
    Set XKBOPTIONS="ctrl:nocaps"

Change keyrate and add redshift to startup
    vim ~/.profile
    
    # Add the following lines
    export EDITOR=/usr/bin/vim
    export BROWSER=/usr/bin/firefox
    xset r rate 180 40
    redshift -l 37.3391655:-121.89472222 &
    
    # Use personal scripts if available github.com/hueyjj/scripts
    xrandr # xrandr list monitors
    xrandr --output eDP1 --off # Turn off laptop
    xrandr --output DP1 --auto --right-of eDP1 # Monitor setup

OR, copy .profile from dotfiles and modify as needed

Change mouse to slower speed
    xinput --list # Find mouse name
    
    xinput --list-props <id> # Verify mouse property

    touch /etc/X11/xorg.conf.d/99-libinput-custom-config.conf
    `
    Section "InputClass"
      Identifier "<something to identify this snippet>"
      MatchDriver "libinput"
      MatchProduct "<substring of the device name>"
      Option "AccelSpeed" "<e.g. 0.3>"
    EndSection
    `
    reboot

Change bash prompt
    .bashrc PS1 variable needs to have '\w' somewhere to display full directory path

Setup hibernation (read everything: swap file, grub modification, kernel settings, regenerating files)
    https://wiki.archlinux.org/index.php/Power_management/Suspend_and_hibernate#Hibernation_into_swap_file

Copy i3 config
    ~/.i3/config # Added volume mixer control

Vim settings from dotfiles

Automount program "udiskie"
    Add `udiskie &` to ~/.xinitrc

Set firefox default browser not just in browser (preferences) but also here:
 Change palemoon to firefox in ~/.config/mimeapps.list
 Change palemoon binding in i3 (if didn't already do so when copying config file) config file

Monitor save kernel settings for monitors
    https://wiki.archlinux.org/index.php/Kernel_mode_setting#Early_KMS_start
    sudo vim /etc/mkinitcpio.conf
    MODULES="i915"  # i915 is for intel integrated graphics

Programs to install:
gvim
firefox
neofetch
redshift
discord
steam (tf2)
visual studio code
pulseaudio
alsa-tools


Setup zsh with oh-my-zsh
